import { notFound } from "next/navigation";
import Link from "next/link";
import { caseStudies } from "@/constants/case-studies";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
export function generateStaticParams(){return caseStudies.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const project=caseStudies.find(p=>p.slug===slug);return {title:project?`${project.title} | Rishav Yadav`:"Project not found",description:project?.scope,alternates:{canonical:`/projects/${slug}`}}}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=caseStudies.find(p=>p.slug===slug);if(!p)notFound();return <><Navbar/><main id="main" className="shell case-page"><Link className="text-link" href="/#projects">← All projects</Link><p className="eyebrow">{p.category} / {p.status}</p><h1>{p.title}</h1><p className="case-lead">{p.focus}</p><div className="tags">{p.techStack.map(t=><span key={t}>{t}</span>)}</div><div className="case-grid"><section><h2>Overview</h2><p>{p.description}</p><h2>My contribution</h2><p>{p.scope}</p>{p.sections?.map(section=><section key={section.title}><h2>{section.title}</h2><p>{section.body}</p></section>)}</section><aside><h3>Project availability</h3><p>{p.note}</p><div className="actions">{p.demo && p.demo !== "#" && <a className="btn primary" href={p.demo} target="_blank" rel="noopener noreferrer">View live report ↗</a>}{p.github&&<a className="btn" href={p.github} target="_blank" rel="noopener noreferrer">View repository ↗</a>}{p.id===4&&<a className="btn primary" href="https://onlinelibrary.wiley.com/doi/10.1002/9781394302994.ch5" target="_blank" rel="noopener noreferrer">Read publication ↗</a>}{p.id===3&&<Link className="btn" href="/">Explore live website ↗</Link>}</div></aside></div><div className="case-contact"><h2>Want to talk engineering?</h2><Link className="btn primary" href="/#contact">Get in touch ↗</Link></div></main><Footer/></>}
