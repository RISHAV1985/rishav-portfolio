import Link from "next/link";
import { ArrowUpRight, Braces, BrainCircuit, Workflow } from "lucide-react";
import { caseStudies } from "@/constants/case-studies";
const icons=[BrainCircuit,Workflow,Braces];
export default function Projects(){return <section id="projects" className="section shell"><div className="section-heading"><div><p className="eyebrow">01 / SELECTED WORK</p><h2>Built to solve.<br/><span>Made to explore.</span></h2></div><p>A closer look at my backend experience, machine learning projects, and work on the web.</p></div><div className="project-grid">{caseStudies.filter(p=>p.featured).map((p,i)=>{const Icon=icons[i];return <article className="project-card" key={p.id}><Link href={`/projects/${p.slug}`} className={`project-cover cover-${i}`} aria-label={`Explore ${p.title}`}><span className="cover-index">0{i+1} / {p.category}</span><Icon size={64} strokeWidth={1}/><span className="cover-title">{p.id===7?<>MODELS / SHAP<br/>TRUSTLENS</>:p.id===8?<>ORDERS / RETRIES<br/>ORCHESTRIX</>:<>API / DATA<br/>PERFORMANCE</>}</span><ArrowUpRight className="cover-arrow"/></Link><div className="project-body"><span className="status">{p.status}</span><h3>{p.title}</h3><p>{p.scope}</p><div className="tags">{p.techStack.slice(0,4).map(t=><span key={t}>{t}</span>)}</div><Link className="text-link project-action" href={`/projects/${p.slug}`}>Explore project <ArrowUpRight size={17}/></Link><div className="project-links">{p.demo && p.demo !== "#" && <a href={p.demo} target="_blank" rel="noopener noreferrer">View live report ↗</a>}{p.github && <a href={p.github} target="_blank" rel="noopener noreferrer">GitHub ↗</a>}</div></div></article>})}</div><details className="more-projects"><summary>More projects & research <span>{String(caseStudies.filter(p=>!p.featured).length).padStart(2,"0")}</span></summary><div className="additional-grid">{caseStudies.filter(p=>!p.featured).map(p=><Link href={`/projects/${p.slug}`} key={p.id}><span className="status">{p.status}</span><h3>{p.title} ↗</h3><p>{p.subtitle}</p></Link>)}</div></details></section>}
